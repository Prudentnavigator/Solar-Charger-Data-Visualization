Help on module main:

NAME
    main - Gui application for visualizing Victron Charger SolarHistory.

CLASSES
    builtins.object
        SolarPlotter

    class SolarPlotter(builtins.object)
     |  SolarPlotter(plot, bar_width)
     |
     |  GUI solar data plotter class.
     |
     |  Methods defined here:
     |
     |  __init__(self, plot, bar_width)
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  add_csv(self) -> None
     |      Add new solar data to solarhistory.csv.
     |
     |  clear_plot(self) -> None
     |      Clear the chart and reset the buttons.
     |
     |  create_gui(self)
     |      Method to create the GUI
     |
     |  create_nav_toolbar(self) -> None
     |      Creates and displays in the GUI a custom navigation toolbar.
     |
     |  data_canvas(self)
     |      Place matplotlib background into a canvas.
     |
     |  draw_plot(self) -> None
     |      Plot the solar data.
     |
     |  gui_buttons(self) -> None
     |      Initiate buttons
     |
     |  gui_labels(self) -> None
     |      Creates GUI labels.
     |
     |  gui_layout(self)
     |      Layout widgets.
     |
     |  plot_battery_max_voltage(self)
     |      Plot the maximum battery voltage and reconfigure
     |      the approriate button.
     |
     |  plot_battery_min_voltage(self)
     |      Plot the minimum battery voltage and reconfigure
     |      the approriate button.
     |
     |  plot_charge_stages(self)
     |      Plot all charge stages and reconfigure the approriate button.
     |
     |  plot_charger_load_consump(self)
     |      Plot the charger load consumption and reconfigure
     |      the approriate button.
     |
     |  plot_daylight(self)
     |      Plot the amount of daylight and reconfigure
     |      the approriate button.
     |
     |  plot_max_power(self)
     |      Plot maximum power of the PV and reconfigure
     |      the approriate button.
     |
     |  plot_pv_max_voltage(self)
     |      Plot the maximum PV voltage and reconfigure
     |      the approriate button.
     |
     |  plot_solar_yield(self)
     |      Plot solar yield and reconfigure the approriate button.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object
     |
     |  ----------------------------------------------------------------------
     |  Data and other attributes defined here:
     |
     |  toolbar = None
     |
     |  win = None

FUNCTIONS
    check_csv_last_upload()
        Check if more data needs to be added.

    close_app()
        When the user clicks the "x" on the top righthand corner
        the program closes.

    main()
        Main entry point.

DATA
    logger = <Logger main (DEBUG)>

FILE
    Victron/main.py


